{
  "name": "recipe-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "turbo run dev",
    "dev:frontend": "turbo run dev --filter=@recipe/frontend",
    "dev:backend": "turbo run dev --filter=@recipe/backend",
    "build": "turbo run build",
    "build:frontend": "turbo run build --filter=@recipe/frontend",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "mobile": "turbo run mobile --filter=@recipe/frontend",
    "store": "turbo run store --filter=@recipe/frontend",
    "generate": "turbo run generate"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
